# railway.toml
# Configuration for Railway deployment
# See: https://docs.railway.app

[build]
builder = "dockerfile"

[deploy]
startCommand = "uvicorn src.paper_digest.ui.app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 30
